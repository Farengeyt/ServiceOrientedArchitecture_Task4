@{
    ViewBag.Title = "Home Page";
}

    <div class="jumbotron">
        <h1>GetStudentsGraterThan</h1>
        <p class="lead">ASP.NET is a free web framework for building great Web sites and Web applications using HTML, CSS and JavaScript.</p>
        @using (Html.BeginForm("GetGraterThan", "Home", FormMethod.Post))
        {
            <p>Enter Mark</p>
            <input type="number" step="0.1" min="2" max="5" name="mark" placeholder="mark" />
            <br />
            <input type="submit" value="Отправить" class="btn btn-primary btn-lg" />
        }
    </div>

    <div class="jumbotron">
        <h1>GetStudentsLowerThan</h1>
        <p class="lead">ASP.NET is a free web framework for building great Web sites and Web applications using HTML, CSS and JavaScript.</p>
        @using (Html.BeginForm("GetLowerThan", "Home", FormMethod.Post))
        {
            <p>Enter Mark</p>
            <input type="number" step="0.1" min="2" max="5" name="mark" placeholder="mark" />
            <br />
            <input type="submit" value="Отправить" class="btn btn-primary btn-lg" />
        }
    </div>

    <div class="jumbotron">
        <h1>GetStudentsInRange</h1>
        <p class="lead">ASP.NET is a free web framework for building great Web sites and Web applications using HTML, CSS and JavaScript.</p>
        @using (Html.BeginForm("GetInRange", "Home", FormMethod.Post))
        {
            <p>Enter Mark</p>
            <input type="number" step="0.1" min="2" max="5" name="MinMark" placeholder="Minimal mark" />
            <br />
            <input type="number" step="0.1" min="2" max="5" name="MaxMark" placeholder="Maximal mark" />
            <br />
            <input type="submit" value="Отправить" class="btn btn-primary btn-lg" />
        }
    </div>